# 项目开发与发布流程

## 环境与基本约定

- 这是 macOS 项目，默认 shell 为 zsh。
- 使用 Python 时必须用 `uv run python`；安装 Python 包时用 `uv pip install`。
- 修改前检查 `git status`，保留用户已有改动，只处理本次任务范围。
- 先读取 `app/build.gradle.kts` 中的 `versionName`、`versionCode`，检查已有 Git 标签和 `dist` 文件；不要凭历史记录猜版本号。
- 构建、签名与发布细节可参考 `docs/publishing.md` 和 `.github/workflows/release.yml`；日常流程以本文件和用户当前指令为准。

## 默认流程

**修改代码 → 验证与测试 → 构建并交付 dist 试用包 → 用户试用确认 → 正式版本打包 → git add / commit / tag / push → 提供发布说明。**

### 1. 修改与验证

- 先定位问题，再做针对性修改；功能逻辑修复应补充有意义的回归测试。
- 功能或统计逻辑修改后运行单元测试和 Release 构建：

  ```bash
  ./gradlew :app:testDebugUnitTest
  ./gradlew :app:assembleRelease
  git diff --check
  ```

- 单纯文案修改不必新增测试或重复全量测试，但应重新构建，并核对 APK 中的实际文案。
- 本机常用 JDK 位于 `/Users/juntao/android-dev/tools/jdk-21.0.12.1+1/Contents/Home`，Android SDK 位于 `/Users/juntao/android-dev/sdk`；使用前确认路径有效。
- 如需运行 Lint，优先在构建结束后单独运行，避免不同变体生成文件并发变化导致分析异常：

  ```bash
  ./gradlew :app:lintDebug --offline --max-workers=1
  ```

- 依赖已缓存时可使用 `--offline`；缺依赖时按实际情况联网获取。不能把依赖缺失或工具异常当作代码验证通过。
- 说明实际执行的检查及其结果。项目的 Lint 可能配置了 `abortOnError=false`，任务成功不等于报告没有错误，必须查看报告。

### 2. 交付 dist 试用包

- 构建产物通常来自 `app/build/outputs/apk/release/app-release.apk`，但交付用户时必须复制到项目根目录的 `dist/`。
- 试用包采用 `slaier-<包内 versionName>-<修复主题>.apk`，例如 `slaier-1.1.1-attendance-stats-fix.apk`；文件名必须与包内版本一致。
- 尚未进入正式发布阶段时，可沿用当前版本号，通过文件名后缀区分试用包。不要仅为交付试用包擅自创建新版本标签。
- 同一修复主题继续调整时，可以更新同名试用包，同时更新对应的 `.apk.sha256` 文件，并明确告知用户下载的是更新后的包。
- 试用包不得覆盖已发布的 `slaier-<版本>.apk` 或该正式版本的校验文件。
- 检查包内 `applicationId`、`versionName`、`versionCode`，验证签名，并与已有正式版签名比较，确保适合覆盖安装。用于覆盖测试的包应使用正式签名，不能悄悄回退到 Debug 签名。
- 交付时给出 `dist` 中 APK 的可点击绝对路径链接，简要说明修改内容、包内版本及验证结果。

### 3. 等待试用确认

- 用户要求“先给测试包”时，交付后等待试用反馈，不提前执行 `git add`、`commit`、`tag`、`push` 或发布 Release。
- 用户确认试用效果并授权发布后，再进入正式发布流程。单纯询问功能、调整文案或要求更新本文件，不表示授权发布当前待提交的修复。
- 用户如果已经明确授权“确认后完成提交、标签和推送”，授权持续有效；满足条件后完成整套流程，不为每条 Git 命令反复询问。
- 试用后如果又有实质性的功能修改，重新测试并交付试用；发布前仅调整版本号或用户指定文案时，完成相应构建验证即可，不额外增加重复审批。

### 4. 正式版本与安装包

- 核对已发布版本和远端标签，遵循用户指定的新版本号。常规兼容性修复可使用补丁版本，例如 `1.1.1 → 1.1.2`。
- 更新 `app/build.gradle.kts`：`versionName` 对应正式发布版本，`versionCode` 必须大于已发布版本，通常递增 1，以支持应用内更新判断。
- 版本号调整后重新构建，不能把旧版本 APK 仅改文件名当成新版本。
- 将正式包放到 `dist/slaier-<版本>.apk`，生成与该文件对应的 `dist/SHA256SUMS.txt`。校验文件中的文件名应为 APK 基名，便于下载后校验。
- 再次核对包内版本和正式签名。保留已有版本产物，不随意清空 `dist`。
- 本地构建和 CI 构建的 APK 校验和可能不同；每个包必须搭配它自身生成的校验文件。

### 5. Git 提交、标签与推送

- 发布前获取远端状态，核对发布分支、远端更新和同名标签；不要覆盖已有发布标签或使用强制推送。
- 使用明确的路径暂存本次经过确认的代码、测试、版本号和相关文档，检查暂存 diff 与 `git diff --cached --check`。
- APK、构建缓存、签名密钥、密码、Cookie、Token、原始个人记录及 HAR 不进入 Git。遵守现有 `.gitignore`，不强制添加 `dist` 或密钥文件。
- 提交说明使用清楚的中文，描述解决的问题、修改后的行为、版本号和验证结果，例如：`发布 v1.1.2：修正考勤统计字段与周明细显示`。
- 在已验证的发布提交上创建附注标签 `v<versionName>`，确认标签指向正确提交。
- 推送发布分支和指定版本标签；远端支持时优先原子推送。不要用 `git push --tags` 顺带发布无关标签。
- 推送后核对结果、分支状态和标签指向。失败时说明具体原因，不宣称发布步骤已完成。

### 6. 发布说明与最终交付

- 推送 `v*` 标签会触发仓库的 Release 工作流。当前工作流生成草稿 Release；除非用户明确要求，否则由用户在 GitHub 检查并正式发布。
- 若尚未核实 CI 完成，只能报告已推送或已触发工作流，不得声称 GitHub APK 或草稿已经生成。
- 发布说明保存到 `docs/releases/v<版本>.md`，并在最终回复中提供可直接复制到 GitHub 的 Markdown 正文和建议标题。
- 发布说明面向使用者，写清修复和体验变化，不堆砌内部实现细节，也不宣称未验证的行为。
- 最终回复包含版本号、`versionCode`、提交短哈希、标签和推送结果、`dist` 安装包与校验文件链接，以及可粘贴的发布说明。
